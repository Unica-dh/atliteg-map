# Backend API Configuration (example)
# Copy this file to .env and configure with your production values

# Frontend API Keys (comma-separated for rotation)
FRONTEND_API_KEYS=your_production_api_key_here

# Next.js Public API Key (must match one from FRONTEND_API_KEYS)
NEXT_PUBLIC_API_KEY=your_production_api_key_here

# Admin Credentials
# Opzione 1 (SEMPLICE): Password in chiaro - basta scriverla direttamente
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_password_here

# Opzione 2 (PIÙ SICURA): Hash bcrypt - per produzione
# Genera con: npm run generate-password-hash
# Poi commenta ADMIN_PASSWORD sopra e decomenta questa:
# ADMIN_PASSWORD_HASH=$2b$10$your_bcrypt_hash_here

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your_jwt_secret_min_32_chars_here

# CORS (comma-separated allowed origins)
ALLOWED_ORIGINS=https://your-domain.com,http://localhost:9000

# API URL for frontend (internal Docker network)
NEXT_PUBLIC_API_URL=http://backend:3001
